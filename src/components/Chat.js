// Imports
import React from "react";
import Nav from "./Nav";
import { ChatEngine } from "react-chat-engine";
import "./Styles/chat"

export default function Chat(porps) {
  return (
    <div>
      <Nav />
    </div>
  );
}
